# Food Order System With Branches

This version supports:
- branch selection
- different WhatsApp number per branch
- different bank account per branch

Edit only config.js:
- branch names
- each branch WhatsApp number
- each branch bank details

When a customer selects a branch:
- that branch account number shows
- that branch WhatsApp receives the order
